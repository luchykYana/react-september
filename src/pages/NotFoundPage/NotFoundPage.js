import css from './NotFoundPage.module.css';

const NotFoundPage = () => {
    return (
        <div className={css.notFound}>
            Sorry, we didn`t find page with this address(
        </div>
    );
};

export {NotFoundPage};
