const baseURL = 'https://jsonplaceholder.typicode.com/';

export const urls = {
    baseURL,
    users: '/users',
    posts: '/posts',
    comments: '/comments',
    albums: '/albums',
    photos: '/photos'
}
